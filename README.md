# Приложение для анализа банковских операций
## Описание:

Приложение для анализа транзакций, которые находятся в Excel-файле. Приложение будет генерировать JSON-данные для 
веб-страниц, формировать Excel-отчеты, а также предоставлять другие сервисы
Установка:

    Кланировать репозиторий и установить зависимости:

pip install poetry
[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.2.3"
requests = "^2.32.3"
python-dotenv = "^1.0.1"
lint = "^1.2.1"
openpyxl = "^3.1.5"
mock = "^5.1.0"

[tool.poetry.dev-dependencies]

[tool.poetry.group.lint.dependencies]
flake8 = "^7.1.1"
black = "^24.10.0"
isort = "^5.13.2"
mypy = "^1.14.1"


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"
pytest-cov = "^6.0.0"

## Использование:

    В PyCharm можно задать нужные транзацкии либо указать путь к вашему excel файлу


## Тестирование функций

    В файле реализовано тестирование фунцкий, размещенных в папке test.
    Для визуализации результатов тестировоания необходимо выполнить следующее.

    В терминале Pycharm набрать команду pytest --cov.

